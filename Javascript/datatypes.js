/* Datatypes in Javascript: 
undefined, null, boolean, string, symbol, number and object 
*/

/* Undefined is something that is not defined yet; It might be a variable that you haven't set to be anything yet. */
var a;
console.log(a);

// Null means nothing;
var b = null;
console.log(b);
// Boolean means true or false;
var cric = true;
console.log(cric);
//A symbol is a immutable primitive value that is unique.

/* String in Javascript is a any sort of text enclosed in Quotation Marks
 be it single '' or Double "" */
 
 String = "string";
 console.log(String);
 
//  Numbers are numbers
var aa = 5;
console.log(typeof(aa));

//Object can store the values in key-value pairs:

var at = {
    k:'king'
}
console.log(at['k']) 